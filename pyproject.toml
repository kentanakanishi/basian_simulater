[project]
name = "basian-simulater"
version = "0.1.0"
description = "Bayesian Optimization Interactive Visualization (GS-TS)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "numpy>=2.4.2",
    "scikit-learn>=1.3",
    "scipy>=1.11",
]
